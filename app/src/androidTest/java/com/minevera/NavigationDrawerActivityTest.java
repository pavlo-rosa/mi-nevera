package com.minevera;
/*
import android.support.test.espresso.contrib.DrawerActions;
import android.support.test.rule.ActivityTestRule;
import android.support.test.runner.AndroidJUnit4;
import android.test.suitebuilder.annotation.LargeTest;

import com.minevera.activities.SignInActivity;

import org.junit.Rule;
import org.junit.Test;
import org.junit.runner.RunWith;

import static android.support.test.espresso.Espresso.onView;
import static android.support.test.espresso.Espresso.pressBack;
import static android.support.test.espresso.assertion.ViewAssertions.matches;
import static android.support.test.espresso.matcher.ViewMatchers.isDisplayed;
import static android.support.test.espresso.matcher.ViewMatchers.withId;
import static android.support.test.espresso.matcher.ViewMatchers.withText;
import static org.hamcrest.Matchers.allOf;
*/
/**
 * Created by Pablo on 28/03/2016.
 */
//@RunWith(AndroidJUnit4.class)
//@LargeTest
public class NavigationDrawerActivityTest {
/*
    public static final String USER_NAME = "Pablo Rosa";
    public static final String USER_EMAIL = "elkomy.pr94@gmail.com";

    @Rule
    public ActivityTestRule<SignInActivity> activityRule = new ActivityTestRule<>(SignInActivity.class);
    /*Partimos de SignInActivity para iniciar sesion automaticamente
    * Probaremos con el usuario elkomy.pr94@gmail.com
    */


    /****
     * PS - 002
     * Test01
     * Descripción: El usuario puede ver su información
     * */
  /*  @Test
    public void testNavigationDrawer01() {
        onView(withId(R.id.drawer_layout)).perform(DrawerActions.open());
        onView(allOf(withId(R.id.ndEmailUser), withText(USER_EMAIL))).check(matches(isDisplayed()));
        onView(allOf(withId(R.id.ndNameUser), withText(USER_NAME))).check(matches(isDisplayed()));
    }
*/
    /****
     * PS - 002
     * Test02
     * Descripción: El usuario puede retroceder
     * */
  /*  @Test
    public void testNavigationDrawer02() {
        onView(withId(R.id.drawer_layout)).perform(DrawerActions.open());
        pressBack();
    }

*/
}
